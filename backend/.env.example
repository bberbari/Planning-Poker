# Copy to .env for local dev. On Render, set FRONTEND_URL in Environment (not in git).
# Comma-separated list if you have preview + production frontends, e.g.:
# FRONTEND_URL=https://yourapp.pages.dev,https://yourapp.netlify.app

FRONTEND_URL=http://localhost:3000
